<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.lynn.springbootstarter.dao.BlogDao">
    <resultMap id="resultMap" type="org.lynn.springbootstarter.model.Blog">
    </resultMap>

    <select id="getUserBlogs" resultMap="resultMap">
        select id,user_id,title,tags,likes,create_time,content
        from t_blog
        where user_id=#{userId}
        order by create_time desc
    </select>

    <select id="getTags" resultType="java.lang.String">
        select distinct(tags) from t_blog
    </select>

    <update id="updateLikes" parameterType="java.lang.Long">
        update t_blog set likes = likes + 1 where id = #{id}
    </update>
</mapper>
